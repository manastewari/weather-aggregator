server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    location /weather/ {
        proxy_pass http://api:8000;
        proxy_read_timeout 30s;
    }
    location / {
        try_files $uri $uri/ /index.html;
    }
}
